// TODO(post-merge): replace with import from ../web/serve.
// This stub exists because Unit 9 was developed in parallel isolation;
// after all PRs land, delete this and use the real module.

import type { Settings } from "./types.ts";

export async function startWebServer(_settings: Settings): Promise<void> {
	throw new Error("web UI not yet wired");
}
